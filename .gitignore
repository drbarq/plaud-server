# secrets & local env — never commit
.env
.env.*
!.env.example
!**/.env.example
*.pem
tokens.json

# python (legacy-mini)
__pycache__/
*.pyc
.venv/

# node
node_modules/

# macOS
.DS_Store

# PoC — the whole folder stays out of the public repo: not just data/ and
# explorer/, but the extraction spec and handoff also embed personal speaker
# calibration (job details, people, personal registers)
poc/
