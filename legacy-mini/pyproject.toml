[project]
name = "plaud-server"
version = "0.1.0"
description = "Self-hosted Plaud pipeline: archive, transcribe, index"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
